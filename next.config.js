/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/x222c.github.io', // Replace with your repo name
  trailingSlash: true,
};

module.exports = nextConfig;